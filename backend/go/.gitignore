### Go build artifacts ###
bin/
build/
dist/
*.exe
*.dll
*.so
*.dylib
*.test
*.out

# Go coverage files
coverage.*
*.coverprofile

### Go modules ###
# Vendor directory (if you ever run `go mod vendor`)
vendor/

### IDE / Editor ###
# VSCode
.vscode/
*.code-workspace

# JetBrains (GoLand)
.idea/

### OS files ###
.DS_Store
Thumbs.db

### Environment and secrets ###
.env
*.local

### Logs ###
*.log

### sqlc ###
# sqlc generates Go files, but DO NOT ignore the generated folder.
# You want to keep the generated code in git.
# HOWEVER sqlc produces metadata files that can be ignored:
sqlc*.json

### Goose migrations ###
# DO NOT ignore migration files, they MUST be committed.
# But ignore goose lock file:
goose.lock

### Docker ###
# Ignore Docker build cache folders
docker-data/
pgdata/
postgres-data/

/internal/db/schema
/internal/db/queries

### Postgres dumps (safe) ###
# Ignore only SQL dumps by naming convention
*.dump.sql
*.backup.sql
dump_*.sql

### Frontend (if any embedded) ###
node_modules/
